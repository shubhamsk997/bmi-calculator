import type { MetadataRoute } from "next"; import { site } from "@/lib/site"; import { articles } from "@/lib/articles"; const paths=["","/about","/bmi-calculator","/bmi-men","/bmi-women","/bmi-by-age","/contact","/privacy-policy","/disclaimer","/terms","/blog",...Object.keys(articles).map(x=>`/blog/${x}`)]; export default function sitemap():MetadataRoute.Sitemap{return paths.map(url=>({url:`${site.url}${url}`,lastModified:new Date("2026-07-24"),changeFrequency:url===""?"weekly":"monthly",priority:url===""?1:.7}))}
