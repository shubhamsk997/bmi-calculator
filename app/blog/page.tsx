import Link from "next/link"; import { articles } from "@/lib/articles"; export const metadata={title:"BMI & Healthy Weight Articles",description:"Evidence-informed guides on BMI, body composition, and sustainable healthy-weight habits."}; export default function Page(){return <section className="container py-16"><p className="text-sm font-bold uppercase tracking-widest text-teal-700">Health library</p><h1 className="mt-3 text-4xl font-extrabold">BMI and healthy weight guides</h1><div className="mt-10 grid gap-5 md:grid-cols-2">{Object.entries(articles).map(([slug,a])=><Link className="card p-6 transition hover:-translate-y-1" href={`/blog/${slug}`} key={slug}><h2 className="text-xl font-bold">{a.title}</h2><p className="mt-3 leading-7 text-slate-600 dark:text-slate-300">{a.description}</p><span className="mt-5 inline-block text-sm font-bold text-teal-700">Read guide →</span></Link>)}</div></section>}
